import { styled } from "styled-components";

const TimeTable = () => {
  return <Wrapper>TimeTable</Wrapper>;
};

export default TimeTable;

const Wrapper = styled.div`
  width: 30%;
  background-color: lightblue;
`;
